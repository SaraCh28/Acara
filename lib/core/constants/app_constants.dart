class AppConstants {
  // 8px design grid
  static const double grid = 8.0;

  // Border radius scale (soft premium)
  static const double borderRadiusSmall = 12.0;
  static const double borderRadiusMedium = 16.0;
  static const double borderRadiusLarge = 24.0;

  // Padding scale
  static const double paddingSmall = grid;
  static const double paddingMedium = grid * 2;
  static const double paddingLarge = grid * 3;
  static const double paddingXLarge = grid * 4;

  static const String appName = 'Acara';
  // Keep font family reference but actual font set in theme where used
  static const String fontFamily = 'Inter';
}
